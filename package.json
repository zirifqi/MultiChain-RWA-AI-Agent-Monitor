{
  "name": "multichain-rwa-ai-agent-monitor",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "test": "turbo run test --filter=@rwa-monitor/alerter",
    "lint": "echo \"No lint targets yet (scaffold phase).\"",
    "typecheck": "echo \"No typecheck targets yet (scaffold phase).\"",
    "db:migrate": "pnpm --filter @rwa-monitor/listener db:migrate",
    "security": "echo \"No security targets yet (scaffold phase).\""
  },
  "devDependencies": {
    "turbo": "^2.1.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild"
    ]
  }
}
